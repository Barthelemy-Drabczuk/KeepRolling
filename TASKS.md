# TASKS.md

Backlog and status tracker for work items. One row per atomic candidate
requirement. Status values: `todo` / `red` / `design` / `green` /
`verify` / `gate` / `done` / `blocked`.

## Epic: NiceGUI frontend redesign

Source: `/home/percevase/.claude/plans/lovely-sauteeing-crystal.md`

| ID | Description | Status | Depends on | Commit | Last status change |
|----|--------------|--------|------------|--------|---------------------|
| FE-1 | Wire NiceGUI into app.py: confirm NiceGUI API via context7, add dependency, mount via ui.run_with() onto the existing FastAPI app, remove old index.html/static/app.js/static/style.css/index_new.html and the /static mount + GET / route, ship a placeholder @ui.page('/') proving the dev server still boots. | done | none | 9bed6c588125b1dfa7bb67dd7093034b8590113b (ARCHITECTURE.md: "FE-1 — Mount NiceGUI onto the FastAPI app, retire the vanilla-JS SPA") | 2026-08-25T23:52:29Z |
| FE-2 | Configure the Quasar colour palette at startup via `app.colors()` with four custom colours matching the reference image's quadrant zones: `high_energy_unpleasant=#783020`, `high_energy_pleasant=#e0c080`, `low_energy_unpleasant=#98a8c0`, `low_energy_pleasant=#a0a888` — verified via `GET /`'s response containing all four in `vue_config`'s `brand` object. | done | FE-1 | facc81084b1ba16eaebef0e3fe496301af63f8bf (ARCHITECTURE.md: "FE-2 — Quadrant colour palette in the Quasar brand config") | 2026-08-26T00:24:24Z |
| FE-3a | Pages `GET /login` and `GET /register` each render a username field, a masked password field, and a submit button (captioned "Log in" / "Register" respectively) styled with FE-2's `high_energy_pleasant` colour (`ui.button(color="high-energy-pleasant")`); each page links to the other. No server-side auth guard on either route in this item — both remain reachable regardless of login state. Verified via the `client` fixture asserting on rendered field/caption/colour text. | done | FE-1, FE-2 | 8e53d35cb9bcaa41bff70be4688de4330650e0ae (ARCHITECTURE.md: "FE-3a — `/login` and `/register` page shape") | 2026-08-26T01:10:36Z |
| FE-3b | On submitting the login form with valid credentials, the page calls `POST /auth/login` and stores the returned `access_token` in `app.storage.user["token"]` and the submitted username in `app.storage.user["username"]`, then navigates to `/` via `ui.navigate.to`. On submitting the register form, the page calls `POST /users`; on success, navigates to `/login` via `ui.navigate.to`. On a 401 from login, shows `ui.notify("Incorrect username or password")` verbatim; on a 400 from register, shows `ui.notify("Username already registered")` verbatim. Register's username/password inputs additionally get client-side length validation (3–50 / ≥8 chars) so the 422 path is normally unreachable; any other non-2xx response (422, 5xx, connection error) from either call shows a fixed generic `ui.notify("Could not complete the request.")` rather than echoing the raw response body — REQ-USER-1's 422 detail includes the submitted password and must never reach the screen. Browser-runtime behavior, not reachable through the `client`/`TestClient` fixture per ARCHITECTURE.md's Testing policy. Manual acceptance (4 enumerated steps, all required): (1) login-success — with the dev server running from `backend/app/`, log in with valid credentials, confirm navigation to `/` and that `backend/app/.nicegui/storage-user-*.json` contains a `token` key with a non-empty JWT and a `username` key matching the submitted username; (2) register-success — register a new user, confirm navigation to `/login`; (3) login-401 — submit wrong credentials, confirm the exact notification text and no navigation; (4) register-400 — register a duplicate username, confirm the exact notification text and no navigation. | done | FE-3a | c85e9b07d4954a69c27d30de46e20ddc6a5ffbff (ARCHITECTURE.md: "FE-3b — Form submission: login/register call the REST API") | 2026-08-26T02:15:00Z |
| FE-4 | The mood pad renders at `/`, replacing FE-1's placeholder label (the page keeps `title="Moodometer"` on its `@ui.page` decorator, so `test_root_page_serves_nicegui_placeholder` needs no change — the title tag alone satisfies it). No auth guard on `/` — consistent with FE-3a's routes; actual authorization is enforced by the backend API once FE-5 submits. The pad is `ui.interactive_image(size=(400, 400), events=["mousedown", "mousemove", "mouseup"], cross=False, content=<SVG>)`, source-less (no `src`). Its SVG `content` renders: four quadrant-filling `<rect>`s coloured `fill="var(--q-{slot})"` referencing FE-2's four custom colour slots by name (top-left=high_energy_unpleasant, top-right=high_energy_pleasant, bottom-left=low_energy_unpleasant, bottom-right=low_energy_pleasant, matching `valence=x/width*2-1`, `energy=1-y/height*2`); seven `<text>` elements with the reference meme's exact captions verbatim (profanity included), placed in their quadrant per this fixed assignment — high-energy/unpleasant: "Fuck it we ball"; high-energy/pleasant: "We are so fucking back", "Let's fucking goooo"; low-energy/unpleasant: "It is what it is", "It's so over", "Mom would be sad" (in that reading order, most-central to most-extreme); low-energy/pleasant: "We vibing" (exact pixel placement within each quadrant is design's to choose); and a marker at the pad's centre, the initial (energy=0.0, valence=0.0) position. A `ui.label` below the pad shows initial text "Energy: 0.00 · Valence: 0.00" (its content updating during drag, and the confirm/submit button and its action, are FE-5 — out of scope here). The pixel→mood coordinate mapping is exposed as a pure function `pixel_to_mood(x: float, y: float, width: float, height: float) -> tuple[float, float]` in `frontend/mood_pad.py`, computing `valence = x/width*2-1`, `energy = 1-y/height*2`, each clamped to [-1.0, 1.0] via `min`/`max`. Verified via: the `client` fixture for everything server-rendered (pad element props — `size`, `events`, `content` containing the four fills/seven captions/marker; the readout label's initial text), and a plain unit test for `pixel_to_mood` (corners map to `(±1, ±1)`, centre maps to `(0, 0)`, out-of-range inputs clamp). Drag-tracking, marker movement, and the readout's live updates remain manual-verification-only per ARCHITECTURE.md's Testing policy — only the *initial* render and the pure math function are in scope here. | done | FE-2 | fe7d04acad6c46d35443b4fc9e60aedca0f89e70 (ARCHITECTURE.md: "FE-4 — The mood pad at `/`") | 2026-08-26T08:59:38Z |
| FE-5 | `mood_pad.create()`'s `/` page gains a `ui.button("Log this mood", color="high-energy-pleasant")` below the readout label, enabled from initial render (the pad's marker already has a valid position — the origin, `energy=0.0`/`valence=0.0`, until dragged — so there's nothing to gate). The drag handler (extended, not replaced) tracks the current `(valence, energy)` in local state and re-renders the readout label live on every drag update — this was explicitly deferred to FE-5 by FE-4's own row. On click, the handler calls `POST /users/{username}/moods` (`{username}` from `app.storage.user["username"]`) with `Authorization: Bearer {app.storage.user["token"]}` (per FE-3b's forward pointer — the stored token has no prefix) and JSON body `{"energy": ..., "valence": ...}` (no `notes`, no `timestamp` — letting REQ-MOOD-2 default it server-side). On 201: `ui.notify("Mood logged!")`, then resets the marker and readout back to the origin. On 401 (missing/expired token — an everyday case, since `/` carries no auth guard and tokens expire in 30 minutes): `ui.notify("Please log in to record a mood.")` and `ui.navigate.to("/login")` — this is post-action navigation after a failed action, not a content guard, so it does not trigger FE-3b's `RedirectResponse` forward pointer. On any other non-2xx (403/422/5xx): a fixed generic `ui.notify("Could not complete the request.")`, never the raw response body, matching FE-3b's established rule. `/` keeps no auth guard — this item does not add one, protecting the ~25 existing tests that fetch `/` with a logged-out `client` fixture. Verified via: the `client` fixture for the button's initial render (caption, colour, not disabled) on `GET /`; manual acceptance for everything else (click → POST → success/failure handling, live readout updates) since it's dispatched over NiceGUI's websocket, unreachable through `TestClient`, per ARCHITECTURE.md's Testing policy. | done | FE-3b, FE-4 | 7e04717f27988975656c0721b9cab847c8e6983c (ARCHITECTURE.md: "FE-5 — Confirm button: log the current mood via `POST /moods`") | 2026-08-26T10:30:00Z |
| FE-6a | The pad gains keyboard focusability: `pad.props('tabindex=0')`, with a generic `.on('keydown', handler, args=['key'])` subscription (bypassing `on_mouse`/`MouseEventArguments`, which structurally cannot carry key identity — confirmed by reading NiceGUI's source, not assumed). On `ArrowUp`/`ArrowDown`/`ArrowLeft`/`ArrowRight`, the handler calls a new pure function `nudge(valence: float, energy: float, key: str) -> tuple[float, float]` in `frontend/mood_pad.py` — Up/Down adjust energy by +0.05/−0.05, Left/Right adjust valence by −0.05/+0.05 (screen-relative: Up = higher energy, Right = more pleasant, matching `pixel_to_mood`'s inverted-y convention), each clamped to [-1.0, 1.0] and rounded to 2 decimal places; any other key is a no-op. The handler routes the result through FE-5's `set_mood()`, so the marker, readout, and stored state stay in lockstep with a drag or a prior nudge. Verified via: a plain unit test for `nudge` (each of the four arrow keys from the origin, clamping at each edge, an unrecognized key is a no-op) and the `client` fixture confirming the pad's `tabindex` prop is `"0"` on `GET /`. The keydown subscription itself, real key-press dispatch, and the resulting marker/readout updates are manual-verification-only per ARCHITECTURE.md's Testing policy — only focusability and the nudge arithmetic are automated here. | done | FE-4, FE-5 | 05bbb8a4921bb547b197b66a0499a4ff1bf97a3f (ARCHITECTURE.md: "FE-6a — Keyboard focus and arrow-key nudging on the pad") | 2026-08-26T15:34:46Z |
| FE-6b | Pressing Enter while the pad has keyboard focus triggers the exact same handler as clicking the "Log this mood" button (FE-5's `log_mood()`) — identical POST, identical 201/401/else branches, identical logged-out short-circuit. No new behavior, no new test target: this is a second trigger for an already-fully-specified action. Entirely manual-verification-only per ARCHITECTURE.md's Testing policy (same category as FE-5's click path). | done | FE-6a, FE-5 | 5885e802e93f5f4c1f886035c3674ba1aff7391e (ARCHITECTURE.md: "FE-6b — Enter on the focused pad submits the mood") | 2026-08-26T22:41:25Z |
| FE-7 | `/history` renders unconditionally (no auth guard, consistent with `/`/`/login`/`/register`) via a new `frontend/history.py` module (`create()`, same per-module convention as `frontend/auth.py`/`frontend/mood_pad.py`). The page checks `app.storage.user.get("token")` at render time: if absent, shows the empty/logged-out prompt "No mood entries yet. Click on the mood board to record your first mood!" (no table); if present, `await`s `frontend/api.py`'s `client()` to fetch `GET /users/{username}/moods?limit=10` with the stored bearer token, and renders the result — newest-first, exactly as returned — in a `ui.table` with columns Timestamp (`%Y-%m-%d %H:%M UTC`, since the server stores naive UTC and there is no reliable client timezone), Energy/Valence (`{value:.2f}`, matching the pad's readout format), Quadrant (derived locally via a new pure function `quadrant_label(energy: float, valence: float) -> str` in `frontend/history.py`, replicating `analytics.get_mood_quadrant_name`'s exact snake_case/neutral-threshold logic — `abs(energy) < 0.1 and abs(valence) < 0.1` — but rendered as Title Case with spaces, e.g. "High Energy Pleasant"/"Neutral" — the sanctioned local-re-derivation pattern per ARCHITECTURE.md's Boundaries, not a round trip), and Notes (shown when present, blank otherwise). A zero-mood logged-in user sees the same empty-state string as the logged-out prompt. On a non-2xx response from the fetch: 401 shows "Please log in to view your mood history." and navigates to `/login` (post-action navigation, not a guard — doesn't trigger FE-3b's `RedirectResponse` forward pointer); any other non-2xx shows a fixed generic "Could not complete the request." inline on the page, never the raw response body. The pad's page (`/`) gains a `ui.link("History", "/history")` for reachability. Moods only — journal entries are FE-8's separate page. Verified via: the `client` fixture for the unguarded route's 200 status and the logged-out prompt text on `GET /history`, the `ui.link`'s `href` prop on `GET /`, and a plain unit test for `quadrant_label` (each of the four quadrants, the neutral band, and its boundary at exactly ±0.1). Everything requiring an authenticated fetch — the populated table, the zero-mood-but-logged-in empty state, and both failure branches — is manual-verification-only per ARCHITECTURE.md's Testing policy, since `app.storage.user` cannot be seeded over HTTP. | done | FE-2, FE-3b | c45aad2e56fc5cd569d5399e1d74828da7fe29d4 (ARCHITECTURE.md: "FE-7 — The mood history page at `/history`") | 2026-08-27T09:00:00Z |
| FE-8a | `/journal` renders unconditionally (no auth guard, consistent with `/`/`/login`/`/register`/`/history`) via a new `backend/app/frontend/journal.py` module (`create()`, same per-module convention as `frontend/auth.py`/`mood_pad.py`/`history.py`), registered from `frontend/__init__.py`'s `create_pages()`. Read-only listing only — composing an entry is FE-8b, linking/displaying a mood is FE-8c. The page reads `app.storage.user.get("username")` and `.get("token")` at render time before the first `await` (FE-7's rule): if either is absent it shows `JOURNAL_EMPTY = "No journal entries yet."` and renders no list; if both are present it `await`s `frontend/api.py`'s `client()` to fetch `GET /users/{username}/entries?limit=10` (`JOURNAL_LIMIT = 10`, matching FE-7's `HISTORY_LIMIT`) with `Authorization: Bearer {token}`, and renders the returned array newest-first exactly as returned (REQ-ENTRY-3 already orders it — no client-side re-sort) as a `ui.column` of one `ui.card` per entry — deliberately not a `ui.table`, since REQ-ENTRY-1 permits 5000-character prose that a table cell truncates. Each card holds exactly two `ui.label`s: the entry's `timestamp` formatted `%Y-%m-%d %H:%M UTC` (same format as FE-7, via a private `_format_timestamp` two-liner duplicated from `frontend/history.py` — second copy, so duplicated rather than extracted; a third consumer is the extraction trigger), then the entry's `content` verbatim. `mood_id` is not rendered in this item and no moods fetch happens here. A logged-in user with zero entries sees the same `"No journal entries yet."` string as the logged-out prompt (FE-7's precedent) — deliberately CTA-free so FE-8b's compose box does not force a wording change that would require editing this item's passing test. On a non-2xx from the fetch: 401 shows `"Please log in to view your journal."` inline and `ui.navigate.to("/login")` (post-action navigation, not a content guard); any other non-2xx (403/5xx/connection error) shows the fixed generic `"Could not complete the request."` inline, never the raw response body. `/` gains `ui.link("Journal", "/journal")` appended as the last statement of `index()`, after FE-7's History link. Verified via: the `client` fixture for `GET /journal`'s 200, the logged-out prompt text on that same response, and the `href="/journal"` prop on `GET /`. The populated list, the zero-entry-but-logged-in empty state, the timestamp format, and both failure branches are manual-verification-only per ARCHITECTURE.md's Testing policy. Out of scope: editing/deleting an entry, pagination/"load more", filtering, search, any nav bar, any auth guard. | done | FE-2, FE-3b, FE-7 | 28ea72ab1c62d51f56413dd25085897c21515e8e (ARCHITECTURE.md: "FE-8a — The read-only journal listing at `/journal`") | 2026-08-27T05:08:48Z |
| FE-8b | `frontend/journal.py`'s `/journal` page gains a compose form above the list, rendered unconditionally for logged-out visitors too (matching `/`'s pad+button, which render logged-out and short-circuit on click): a `ui.textarea(label="New journal entry")` and a `ui.button("Save entry", color="high-energy-pleasant")` (FE-3a/FE-5's colour). FE-8a's list moves into a `@ui.refreshable` function so a successful save re-renders it without a page reload (confirm `@ui.refreshable`'s current API via context7 first, per CLAUDE.md's Dependency changes). On click, the handler validates client-side first — if the textarea's stripped value is empty or longer than 5000 characters, `ui.notify("Journal entry must be between 1 and 5000 characters.")` and no request is sent (keeps REQ-ENTRY-1's 422 path normally unreachable, FE-3b's established rule); the textarea also carries `.props("maxlength=5000")` as a hard input-level cap (confirm Quasar `QInput` honours `maxlength` via NiceGUI's wrapper rather than assuming). If `app.storage.user` has no `username`/`token`, short-circuit with `ui.notify("Please log in to write a journal entry.")` and `ui.navigate.to("/login")` before building any URL (same rationale as FE-5's `log_mood()`). Otherwise `POST` to `/users/{username}/entries` with the bearer token and JSON body `{"content": <stripped text>}` — no `timestamp`, no `mood_id` (FE-8c). On 201: `ui.notify("Journal entry saved!")`, clear the textarea, refresh the list. On 401: `ui.notify("Please log in to write a journal entry.")` and `ui.navigate.to("/login")`. On any other non-2xx (403/422/5xx): the fixed generic `ui.notify("Could not complete the request.")`, never the raw response body. FE-8a's `"No journal entries yet."` empty prompt stays verbatim (pinned by `tests/test_frontend.py`). Verified via: the `client` fixture asserting the textarea's `label` and the button's caption/`color` prop are present on a logged-out `GET /journal`. The click path, the POST, all three response branches, the client-side validation notify, the textarea clear, and the list refresh are manual-verification-only. Manual acceptance (5 steps, all required): (1) save-success; (2) empty-content validation; (3) logged-out click → login notify + navigation; (4) expired-token 401 → same; (5) reload persistence. | done | FE-8a | 65fdbde (ARCHITECTURE.md: "FE-8b — The journal compose form at `/journal`") | 2026-08-27T10:04:45Z |
| FE-8c | `/journal` gains optional mood linking (REQ-ENTRY-5) and linked-mood display. After the entries fetch succeeds (and only then — if FE-8a's 401 or generic branch fired, the page has already returned and no moods fetch happens), the page `await`s a second call, `GET /users/{username}/moods?limit=100` (`MOOD_WINDOW = 100`, the API's own default limit), and builds a `{id: mood}` dict from the result. Two consumers: (a) the compose form gains a `ui.select(label="Link a mood")` whose options come from that dict via a new pure function `mood_option_label(mood: dict) -> str` returning `f"{_format_timestamp(mood['timestamp'])} — {quadrant_label(mood['energy'], mood['valence'])}"`, plus a `None` option captioned `"No linked mood"` (the default); when a mood is selected, FE-8b's POST body gains `"mood_id": <selected id>`, otherwise the key is omitted entirely. (b) Each list card gains a third `ui.label` from a new pure function `mood_line(mood: dict | None) -> str` — `""` for `None`, otherwise `f"Mood: {quadrant_label(mood['energy'], mood['valence'])} (Energy: {mood['energy']:.2f} · Valence: {mood['valence']:.2f})"`; the label is only created when non-empty, so a `null` `mood_id` — or a linked mood outside the 100-mood window, or one unlinked per REQ-ENTRY-6 — renders no third label. `quadrant_label` is imported from `frontend.history` (`from .history import quadrant_label`), not re-copied — a sibling `frontend/` import, not a Boundaries violation. Graceful degradation: if the moods fetch returns non-2xx while entries succeeded, render the list with no mood lines and the select with only `"No linked mood"`, and show no error message. Verified via: plain unit tests in `tests/test_journal.py` for `mood_line` (`None` → `""`; a known mood → the exact string; a neutral mood → `"Neutral"`; 2-decimal formatting) and `mood_option_label` (exact string for a known mood). The select's population, `mood_id` reaching the POST body, the mood line on populated cards, the null/outside-window cases, and the degradation branch are manual-verification-only. Out of scope: changing an existing entry's link, unlinking, any N+1 per-entry mood fetch. | gate | FE-8b, FE-7 | | 2026-08-27T12:24:00Z |
| FE-9 | Analytics/charts page (ui.echart for trend/distribution/scatter views). Each quadrant's chart series uses FE-2's corresponding zone colour. Renders at `/analytics`. | todo | FE-2, FE-3b | | 2026-08-26T01:00:00Z |
| FE-10 | Export page (CSV/JSON/PDF download via ui.download). Renders at `/export`. | todo | FE-2, FE-3b | | 2026-08-26T01:00:00Z |
| FE-11 | Docs sync: update CLAUDE.md's Frontend section and Commands section if the dev-server invocation changed. | todo | FE-1, FE-2, FE-3a, FE-3b, FE-4, FE-5, FE-6a, FE-6b, FE-7, FE-8a, FE-8b, FE-8c, FE-9, FE-10, FE-12 | | 2026-08-27T09:39:00Z |
| FE-12 | Extract `auth_headers(token: str) -> dict[str, str]` returning `{"Authorization": f"Bearer {token}"}` into `frontend/api.py` and route all authenticated call sites through it — `mood_pad.log_mood()` (FE-5), `history.history()` (FE-7), `journal` (FE-8a/FE-8b). Pure refactor, no behavior change, no new page: discharges FE-5's forward pointer (ARCHITECTURE.md, FE-7 entry), whose three-call-site trigger FE-8a is the first item to meet. `api.client()` keeps its no-argument signature. Verified via a plain unit test for `auth_headers` plus the existing suite staying green with an unchanged pass count. | todo | FE-8a | | 2026-08-27T09:39:00Z |
